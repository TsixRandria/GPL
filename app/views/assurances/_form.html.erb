<%= form_with(model: assurance, local: true) do |form| %>
  <% if assurance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(assurance.errors.count, "error") %> prohibited this assurance from being saved:</h2>

      <ul>
        <% assurance.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :assurance %>
    <%= form.text_field :assurance %>
  </div>

  <div class="field">
    <%= form.label :montant %>
    <%= form.text_field :montant %>
  </div>

  <div class="field">
    <%= form.label :caution %>
    <%= form.text_field :caution %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
